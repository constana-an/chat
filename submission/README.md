# A1 submission

| File | Step |
| --- | --- |
| `as-built.pdf` | 1 — as-built diagram of the notification system |
| `alternative-design.pdf` | 2 — subscription-based alternative |
| `prediction-and-change-record.md` | 3 and 4 |
| `reflection.md` | 5 — written without AI |
| `transcripts/` | one transcript per working session — **required** |

The project code sits in the repository root (`server/`, `public/`, `tests/`).
Run `npm test` — no dependencies to install.
